<div 
    class="{{bem "split-view" (concat "$" this.axis)}} {{bem (bem "data-table") "head"}}"
    {{did-insert this.didInsert}}
    {{did-update this.didUpdateAttrs 
      @barSize @responsive @minSize 
      @minSizes @fluent @sizes}}
  >
    <DataTable::HeadPanel 
      @axis={{this.axis}}
      @barSize={{this.barSize}}
      @addChild={{this.addChild}}
      @removeChild={{this.removeChild}}
      @hasItemsSource={{this.hasItemsSource}}
      @itemTemplateName={{this.itemTemplateName}}
      @itemContainerGenerator={{this.itemContainerGenerator}}
      as |Columns|
    >
      {{#unless this.hasItemsSource}}
        {{yield Columns}}
      {{/unless}}
    </DataTable::HeadPanel>
    
  </div>
