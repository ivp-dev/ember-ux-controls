<div 
  class="{{this.classNames}}"
  {{did-insert this.didInsert}}
  {{did-update this.didUpdateAttrs 
    @axis @sizes @barSize @minSize 
    @minSizes @fluent @responsive
  }}
  ...attributes
  >
  <SplitView::Panel
    @axis={{this.axis}}
    @barSize={{this.barSize}}
    @addChild={{this.addChild}}
    @removeChild={{this.removeChild}}
    @hasItemsSource={{this.hasItemsSource}}
    @itemTemplateName={{this.itemTemplateName}}
    @itemContainerGenerator={{this.itemContainerGenerator}}
  >
    {{#unless this.hasItemsSource}}
      {{#with (hash Pane=(component 
        this.itemTemplateName
        content=@content
        axis=this.axis
        barSize=this.barSize
        addChild=this.addChild
        removeChild=this.removeChild
        lastItem=this.items.lastObject
        hasItemsSource=this.hasItemsSource)) as |Panes|
      }} 
        {{yield Panes}}
      {{/with}}
    {{/unless}}
  </SplitView::Panel>
</div>
