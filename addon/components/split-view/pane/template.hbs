<div 
  id="{{this.elementId}}" 
  class="{{this.classNames}}" 
  {{add-child this}}
>
  {{#if this.hasItemsSource}}
    {{this.content}}
  {{else}}
    {{yield}}
  {{/if}}
</div>

{{#unless this.isLast}}
  <SplitView::Bar
    @axis={{this.axis}} 
    @barSize={{this.barSize}}
  />
{{/unless}}