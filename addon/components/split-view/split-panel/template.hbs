{{#if this.isItemsHost}}
  {{#each this.children as |pane|}}
    <SplitView::Pane 
      @barSize={{this.barSize}}
      @axis={{this.axis}}
      @visualParent={{this}}
      @pane={{pane}}
    />
  {{/each}}
{{else}}
  {{yield}}
{{/if}}