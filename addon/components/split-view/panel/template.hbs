{{#if this.isItemsHost}}
  {{#each this.children as |pane|}}
    <SplitView::Pane 
      @pane={{pane}}
      @axis={{this.axis}}
      @barSize={{this.barSize}}
      @lastItem={{this.children.lastObject}}
    />
  {{/each}}
{{else}}
  {{yield}}
{{/if}}