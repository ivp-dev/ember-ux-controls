<div 
  class={{this.classNames}}
  {{did-update 
    this.didUpdateAttrs 
    @barSize
    @minSize
  }}
  {{split-view-modifier
    host=this
    axis=this.axis
    classNamesBuilder=this.classNamesBuilder
    minSizes=@minSizes
    sizes=@sizes
    barSize=@barSize
    fluent=@fluent
    responsive=@responsive
    onSizeChanged=@onSizeChanged
  }}
  ...attributes
  >
  {{yield (hash Pane=(
    component "split-view/pane"
    content=@content
    classNamesBuilder=this.classNamesBuilder))
  }}
</div>