<div class={{this.classNames}}>
  {{#if this.hasItemsSource}}
    {{#each this.children as |item|}}
      <TreeView::Item 
        @header={{item.header}}
        @itemsSource={{item.items}}
        @parentElement={{this}}
      />
    {{/each}}
  {{else}}
    {{yield}}
  {{/if}}
</div>